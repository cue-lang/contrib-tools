# Generated by internal/ci/ci_tool.cue; do not edit

name: Test
env:
  CUECKOO_PAT: ${{ secrets.cueckooPAT }}
defaults:
  run:
    shell: bash
on:
  push:
    branches:
    - '*'
    tags-ignore:
    - v*
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Set build branch env vars
      run: |-
        git config user.email cueckoo@cuelang.org
        git config user.name cueckoo
        git config http.https://github.com/.extraheader "AUTHORIZATION basic $(echo cueckoo:$CUECKOO_PAT | base64)"
        git config -l
    - name: Create a new branch
      run: |-
        git remote -v
        git branch -a
        git checkout -b asdf
        echo '# This is a test' >> .github/workflows/test.yml
        git add -A
        git commit -am 'A test'
        git push https://cueckoo:$CUECKOO_PAT@github.com/$GITHUB_REPOSITORY asdf

