# Test that the pre-command hook does nothing when already at latest.

env _CUECKOO_VERSION_OVERRIDE=v0.99.0
exec cueckoo rewrite-commit-msg -m msg commit.txt
! stderr 'updating'

-- commit.txt --
original
